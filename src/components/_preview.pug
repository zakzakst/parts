doctype html
html(lang="ja")
  head
    meta(charset="utf-8")
    meta(name='description', content='パーツ集')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    link(media='all', rel='stylesheet', href=path('/css/style.css'))
    script(src='https://use.fontawesome.com/releases/v5.14.0/js/all.js', defer)
  body
    div(style='padding: 16px')
      != yield
